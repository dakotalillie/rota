pre-commit:
  parallel: true
  commands:
    format-check-ui:
      glob: "ui/**/*.{ts,tsx}"
      run: task format:check:ui
    lint-ui:
      glob: "ui/**/*.{ts,tsx}"
      run: task lint:ui
    test-ui:
      glob: "ui/**/*.{ts,tsx}"
      run: task test:ui
